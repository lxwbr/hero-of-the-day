service: hero-of-the-day
provider:
  name: aws
  runtime: rust
  memorySize: 1024
  region: eu-central-1
  stage: dev
  environment:
    HEROES_TABLE: ${self:service}-${self:provider.stage}-heroes
    GOOGLE_CLIENT_ID: ${env:GOOGLE_CLIENT_ID}
    HOSTED_DOMAIN: ${env:HOSTED_DOMAIN}
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:*
      Resource:
        - "Fn::GetAtt": [ HeroesTable, Arn ]

custom:
  rust:
    dockerTag: '0.2.6-rust-1.41.0'

package:
  individually: true

plugins:
  - serverless-rust
  - serverless-pseudo-parameters

resources:
  Resources:
    HeroesTable:
      Type: AWS::DynamoDB::Table
      Properties:
        AttributeDefinitions:
          - AttributeName: name
            AttributeType: S
        KeySchema:
          - AttributeName: name
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST
        SSESpecification:
          SSEEnabled: true
        TableName: !Sub ${self:provider.environment.HEROES_TABLE}